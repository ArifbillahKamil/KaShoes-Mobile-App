<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration for development.
    Allows cleartext HTTP traffic to localhost / local network.
    IMPORTANT: For production, remove localhost domain and use HTTPS only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator loopback to host machine -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- Common local network ranges -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <!-- localhost for device testing with port forwarding -->
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
